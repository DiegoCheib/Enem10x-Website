import React from 'react'
import ReactDOM from 'react-dom/client'
import Page1 from './components/Home-page'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    < Page1 />
  </React.StrictMode>,
)
